name: Build and Release

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.7.3'
          arch: win64_msvc2019_64
          modules: 'qtmultimedia'

      - name: Configure
        run: cmake -S Podcast -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Package Windows
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path stage
          Copy-Item build/Release/PodcastClient.exe stage/
          Copy-Item build/Release/PodcastServer.exe stage/
          windeployqt stage/PodcastClient.exe
          windeployqt stage/PodcastServer.exe
          Compress-Archive -Path stage/* -DestinationPath Podcast-win-x64.zip

      - uses: actions/upload-artifact@v4
        with:
          name: win-build
          path: Podcast-win-x64.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev libxkbcommon-dev libegl1-mesa-dev

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.7.3'
          modules: 'qtmultimedia'

      - name: Configure
        run: cmake -S Podcast -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --parallel

      - name: Package Linux
        run: |
          QT_ROOT=$(dirname $(dirname "$(which qmake)"))
          echo "Qt root: $QT_ROOT"
          mkdir -p stage/lib stage/plugins
          cp build/PodcastClient build/PodcastServer stage/

          # Qt-библиотеки (|| true = пропустить, если файла нет)
          cp -P $QT_ROOT/lib/libQt6Core.so*       stage/lib/ || true
          cp -P $QT_ROOT/lib/libQt6Gui.so*        stage/lib/ || true
          cp -P $QT_ROOT/lib/libQt6Widgets.so*    stage/lib/ || true
          cp -P $QT_ROOT/lib/libQt6Network.so*    stage/lib/ || true
          cp -P $QT_ROOT/lib/libQt6Multimedia.so* stage/lib/ || true
          cp -P $QT_ROOT/lib/libQt6DBus.so*       stage/lib/ || true
          cp -P $QT_ROOT/lib/libQt6XcbQpa.so*     stage/lib/ || true
          cp -P $QT_ROOT/lib/libav*.so*           stage/lib/ || true

          # Системные библиотеки
          cp -P /usr/lib/x86_64-linux-gnu/libicui18n.so*   stage/lib/ || true
          cp -P /usr/lib/x86_64-linux-gnu/libicuuc.so*     stage/lib/ || true
          cp -P /usr/lib/x86_64-linux-gnu/libicudata.so*   stage/lib/ || true
          cp -P /usr/lib/x86_64-linux-gnu/libstdc++.so.6*  stage/lib/ || true
          cp -P /usr/lib/x86_64-linux-gnu/libgcc_s.so.1    stage/lib/ || true
          cp -P /usr/lib/x86_64-linux-gnu/libdouble-conversion.so* stage/lib/ || true
          cp -P /usr/lib/x86_64-linux-gnu/libpcre2-16.so*  stage/lib/ || true

          # Плагины Qt
          cp -rP $QT_ROOT/plugins/platforms    stage/plugins/ || true
          cp -rP $QT_ROOT/plugins/imageformats stage/plugins/ || true
          cp -rP $QT_ROOT/plugins/iconengines  stage/plugins/ || true
          cp -rP $QT_ROOT/plugins/styles       stage/plugins/ || true
          cp -rP $QT_ROOT/plugins/multimedia   stage/plugins/ || true

          # Скрипты запуска
          printf '#!/bin/bash\nDIR="$(cd "$(dirname "$0")" && pwd)"\nexport LD_LIBRARY_PATH="$DIR/lib:$LD_LIBRARY_PATH"\nexport QT_PLUGIN_PATH="$DIR/plugins"\nexec "$DIR/PodcastClient" "$@"\n' > stage/run_client.sh
          printf '#!/bin/bash\nDIR="$(cd "$(dirname "$0")" && pwd)"\nexport LD_LIBRARY_PATH="$DIR/lib:$LD_LIBRARY_PATH"\nexec "$DIR/PodcastServer" "$@"\n' > stage/run_server.sh
          chmod +x stage/run_client.sh stage/run_server.sh

          echo "=== Содержимое lib ==="
          ls stage/lib | head -40

          tar -czf Podcast-linux-x64.tar.gz -C stage .

      - uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: Podcast-linux-x64.tar.gz

  release:
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare files
        run: |
          mkdir out
          cp artifacts/win-build/Podcast-win-x64.zip out/
          cp artifacts/linux-build/Podcast-linux-x64.tar.gz out/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: "Release #${{ github.run_number }}"
          files: |
            out/Podcast-win-x64.zip
            out/Podcast-linux-x64.tar.gz